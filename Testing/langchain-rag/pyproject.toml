[project]
name = "langchain-rag"
version = "0.1.0"
description = ""
authors = [
    {name = "sean22-blip",email = "ieandxd@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
]

[tool.poetry]
packages = [{include = "messages", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
